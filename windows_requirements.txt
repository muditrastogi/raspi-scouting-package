# Windows Scouting Package Requirements
# Dependencies for camera streaming, recording, and Windows integration

# Computer Vision (Required)
opencv-python>=4.5.0
Pillow>=8.0.0
numpy>=1.19.0

# GUI Framework (Usually pre-installed with Python)
# tkinter - comes with Python installation

# Windows Integration (Optional but recommended)
pywin32>=300; sys_platform == "win32"

# Note: This is a minimal setup for the Windows version
# All Flask/web dependencies removed for simplicity
